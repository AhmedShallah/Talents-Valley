export const validfName = new RegExp(/\S[A-Za-z]/gi);

export const validlName = new RegExp(/\S[A-Za-z]/gi);

export const validEmail = new RegExp(/\s[A-Za-z]+{@}gmail.com/gi);

export const validPassword = new RegExp(/^[A-Z]+[a-z]*{\W}+]/gi);

export const validMobil = new RegExp(/^{+}[0-9]/gi);

export const validCountry = new RegExp(/^[A-Za-z]/gi);
